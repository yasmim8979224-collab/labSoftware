// Exercícios 3 - Variáveis

// Crie as variáveis:

// produto
// preco
// quantidade

// Mostre no console usando concatenando as variáveis:
// Meu (produto) custa (preco) e temos (quantidade) no estoque
let produto = "Acucar";
let preco = 15.0;
let quantidade = 2;
console.log(
  "Meu %s custa %d e temos %d no estoque ",
  produto,
  preco,
  quantidade,
);
